###

  Topic model tests

###
module 'Topic Model',
  setup: ->
    Openedui.reset()

test 'Sanity check', ->
  ok(Openedui.Topic, 'the topic model exists')

test 'displayingStandard is correct', ->
  ok(true)

test 'standardGroup is correct', ->
  ok(true)

test 'grade is correct', ->
  ok(true)